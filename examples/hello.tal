( test file )

|0100 @program

	;hello-word print-str
	#800f DEO

BRK

@print-str ( str* -- )

	&while
		( send ) LDAk #18 DEO
		( loop ) INC2 LDAk ?&while
	POP2

JMP2r

( #122325 ) ( number )
( |0080 #12 ) ( rewind )
( .what ) ( reference )
( @1234 ) ( invalid )
( ,label2 ) ( distance err )
( @label2 ) ( duplicate )

$100 @label2

~examples/include.tal

